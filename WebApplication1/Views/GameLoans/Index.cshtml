@model IEnumerable<GameLib.Models.GameLoan>

@{
    ViewData["Title"] = "Index";
}

<h1>Видачі</h1>

<p>
    <a asp-action="Create">Додати нову видачу</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Дата
            </th>
            <th>
                Гра
            </th>
            <th>
                Користувач
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Game.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Fullname)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.LoanId">Редагувати</a> |
                    <a asp-action="Details" asp-route-id="@item.LoanId">Деталі</a> |
                    <a asp-action="Delete" asp-route-id="@item.LoanId">Видалити</a>
                </td>
            </tr>
        }
    </tbody>
</table>
